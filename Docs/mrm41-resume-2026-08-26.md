# MRM-16 / MRM-41 / MRM-42 — resume prompt (2026-08-26, after the first working session)

This supersedes `Docs/mrm41-items-interaction-kickoff.md` for a *fresh* session — that doc was the
from-scratch kickoff; this one describes what already exists so a new session doesn't re-derive it
or, worse, re-start from zero. **Do not treat pasting this prompt as a signal to start
implementing** — read it, confirm you understand the state, and wait for Carlos's explicit go-ahead
before writing code or touching Unity/Blender. See `[[feedback_dont_auto_start_on_kickoff]]` in
memory — this was a real correction from the first session on this branch.

---

## Prompt to paste

> Resuming **Mr. Moonlight**, branch `mrm-41`. A prior session already did a full working pass on
> MRM-16/41/42 together (Carlos's deliberate one-branch exception, see
> `Docs/mrm41-items-interaction-kickoff.md`). **Read this doc fully before doing anything** — do not
> start implementing, testing, or touching Unity/Blender until Carlos explicitly says to. He
> specifically corrected a prior session for starting work immediately off a kickoff prompt instead
> of waiting for his go-ahead and for the 3D prop model locations he still needs to hand over.
>
> **State as of 2026-08-26, end of the first session:**
>
> - `mrm-41` has `main` merged in (commit `c0aa477`, clean, no conflicts) — `main`'s MRM-18 merge
>   (`d36cc4f`) is present. Confirm with `git log --oneline --graph -6` that this is still true; if
>   `main` has moved further since, merge it in again before anything else (same as last time —
>   `git merge main --no-edit`, expect a clean merge).
> - **Update, 2026-08-26 later same day: the code below is now committed** on `mrm-41`
>   (`45de5df`, "Add MRM-16/41/42 interaction, inventory, and item framework") — Carlos committed
>   it himself via GitHub Desktop using a summary Claude prepared. **Still not merged to `main`.**
>   He intends to merge it as a checkpoint despite the catalogue/scene-wiring below being
>   unfinished — that's a deliberate exception, not a sign the work is done. Run `git log --oneline
>   -3` and `git status` to confirm current state before assuming anything below is stale.
> - **Also as of that update: he's pausing to reconsider the 3D asset pipeline itself** before
>   handing over prop model locations — a new Linear issue for that decision is expected but not
>   yet created. Don't chase him for props until that's resolved.
> - **Linear cleanup, same day:** MRM-41 (and MRM-18, MRM-70) had been silently auto-closed to
>   Done by a Linear workflow automation ("On PR merge, move to Done") that fired on unrelated
>   checkpoint merges — all three were reopened (MRM-41 → In Progress) and the automation itself
>   was fixed (now moves to In Review, not Done, on merge). See `Docs/changelog.md`'s MRM-18 entry
>   for the detail; don't rely on Linear status alone without checking actual acceptance criteria
>   — see `[[linear_status_not_completion]]` in memory.
>
> **MRM-16 — fully built, compiles clean, NOT scene-tested:**
> - `Assets/_Project/Code/Runtime/Interaction/Interactable.cs` — `Interactable` component + `InteractionType` enum
> - `Assets/_Project/Code/Runtime/Interaction/InteractionDetector.cs` — proximity+aim detection, disambiguation, shared fade, highlight via `MaterialPropertyBlock` emission
> - `Assets/_Project/Code/Runtime/UI/InteractionPromptUI.cs` — unstyled placeholder prompt (TMP text, no icon art yet)
>
> **MRM-41 — framework fully built, compiles clean, NOT scene-tested, catalogue NOT created yet:**
> - `Assets/_Project/Code/Runtime/Items/ItemId.cs`, `ItemDefinition.cs` (+ `ItemCategory`), `ItemEffectApplier.cs`, `Item.cs`, `Inventory.cs`
> - `Assets/_Project/Code/Runtime/UI/InventoryFeedbackUI.cs` — the "storage full" refusal toast
> - `PlayerStats.cs` — added `Drunkenness`/`WeedHigh`/`MorphineHigh` stat pools (pool only, no gameplay consequence yet — that's a future issue)
> - `PlayerController.cs` — added `Input` accessor (exposes its `InputMapController` for MRM-16/42 to read Gameplay actions without a second instance) and `SetMovementLocked()` (reversible, for MRM-42)
> - **No `ItemDefinition` assets exist yet** — the 9-item demo catalogue (Crackers, Soda, Bandages, Vodka, Beer, Blunt, Morphine, Pistol ammo, Shotgun shells) plus the 9 equipment types are enum values in `ItemId` but have no ScriptableObject assets or props in the scene. That's the next real step, and it's blocked on Carlos's prop models (see below).
>
> **MRM-42 — mechanics only, no layout, still blocked:**
> - `Assets/_Project/Code/Runtime/UI/InventoryUIController.cs` — open/close/navigate/use state machine, stays in the `Gameplay` input map (confirmed this is correct per the MRM-8 comment thread — the actions it needs live there), reversible movement lock, force-closes via the existing `HudCloseRequest` hook MRM-17 already raises on death
> - **Still blocked on Carlos's mockup image** — checked MRM-42's Linear attachments twice this session, still `[]`. Do not guess the layout. Check again before assuming it's still blocked.
>
> **Verification done so far:** code compiles with zero errors/warnings (`refresh_unity` + `read_console` via the UnityMCP bridge). **Nothing has been verified live in Play Mode and nothing exists in any scene** — no GameObjects, no prefabs, no `ItemDefinition` assets. The acceptance criteria (prompt fade, highlight, disambiguation, pickup/storage/refusal, etc.) are all still unverified.
>
> **What's actually blocking forward progress:**
> 1. Carlos is providing 3D prop model locations for the item catalogue (and possibly MRM-16 test props) — **wait for these**, don't build placeholder-primitive test rigs preemptively unless he says to.
> 2. Once props are in hand: same Blender/Unity pipeline MRM-70 used for vegetation (1 unit = 1m, apply transforms, origin at feet/handhold, FBX Apply Transform checked; then Unity import + a per-prop material call, `URP/Lit` vs. `RetroLit.shader` — ask Carlos which per prop). **Ask permission before each piece of actual Blender/Unity work**, then do it, verify by reading real state back, document. Standing rule, not new.
> 3. MRM-42's mockup — still needed before any Canvas/layout work starts on that issue specifically. MRM-16/41 don't depend on it.
>
> **Standing rules, unchanged:** one shared branch/PR for all three issues (Carlos's deliberate exception); never commit/push; ask before Unity/Blender scene work, then verify + document; and — the correction from last session — **a kickoff/resume prompt is context to read, not a start signal.** Wait for Carlos's explicit go before doing anything below this line.
