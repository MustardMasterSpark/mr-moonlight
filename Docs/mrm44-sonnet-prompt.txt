*** UPDATED 2026-09-19 (read this block first; the sections below are the 2026-09-18 first-iteration handoff) ***
Since then: the beam is tunable (Flashlight* tunables + Live tuning), Carlos saved his numbers (intensity 60, range 40,
outer 70, inner 25, colour #FFE094, shadows none, cookie on); the hands and weapons were reworked onto a URP `ViewModel`
rendering layer with their own sun-following directional light and world-light scan; the HQ mask maps were restored;
SessionLog v4 records the flashlight state. Everything about lighting is now in Docs/lighting.md and
Docs/viewmodel-light-layers.md. Commit message for this batch: Docs/mrm44-commit-message.txt.
STILL OPEN IN MRM-44 (unchanged, none started): beam sway (idle / walking / sprinting), visible breath, enemy visual
detection reading MoonlightFlashlight.IsOn (raise with Carlos first; CLEANUP-adjacent), the chest lamp model and Scott's
Sharpie texture (art from Carlos), a performance check in a build (H9 / H10 in Docs/performance-sessions.md).
The NEXT SESSION is the lighting rework (TimeManager + fog + skyboxes + light sources), now Linear MRM-86 / branch mrm-86:
Docs/lighting-rework-opus-prompt.txt. The remaining MRM-44 scope above stays on MRM-44 / branch mrm-44. New branches: --no-track (CLAUDE.md).
Item 2 below ("tune the beam") is DONE, except the fog look, which is logged on MRM-67.
================================================================================================================

Context prompt: MRM-44 FLASHLIGHT + VISIBLE BREATH (Sonnet). Read this cold; don't rely on chat scrollback.

WRITTEN: 2026-09-18. Branch: `mrm-44` (local, cut from origin/main 087c0b4; NOT committed, Carlos commits in GitHub Desktop).
Read also: CLAUDE.md, Docs/interim-small-tasks-prompt.txt (rules + work log), Linear MRM-44 (source of truth; my
2026-09-18 comment lists what is done).

===========================================================================
1. WHAT IS DONE (first iteration)
===========================================================================
- F (keyboard) / D-Pad Up (gamepad) toggles a cone of light. Instant, NO animation, NO arm, NO wieldable.
  Carlos, verbatim: "When we activate the flashlight we don't have any animation or anything. We just have the beam of
  light." The lamp will later be a model fixed on the CHEST of Tracey's body, not a hand-held tool.
- Code: `Assets/_Project/Code/Runtime/Player/MoonlightFlashlight.cs` (on the light object; finds MoonlightPlayerRig in
  a parent; reads `Actions.Gameplay.FlashlightToggle`; `IsOn`, `SetOn(bool)`, `Toggled` UnityEvent<bool>).
- Prefab: child `Flashlight` under `Player_Tracey/Body/Head/Camera` in `Assets/_Project/Prefabs/Player/Player_Tracey.prefab`
  (Spot Light range 30, spotAngle 60, inner 25, intensity 4, colour (1,.94,.8), shadows None, cookie =
  `ThirdParty/AST-046/HQFPS/Art/Meshes/Wieldables/Flashlight/Materials/Flashlight_Cookie.png`). Read back and confirmed
  after save. Island, Island_Legion, Sandbox and the vegetation galleries inherit it.
- Input asset: F rebound from Interact to FlashlightToggle; old L binding deleted. INTERACT NOW HAS NO KEYBOARD BINDING
  (Carlos: "I'll think of where to map the interaction button later"). Gamepad Interact (X) unchanged. Docs/controls.md and
  Docs/input-map.md updated. Wrapper `InputSystem_Actions.cs` regenerated by Unity (verified).

===========================================================================
2. WHAT IS NOT DONE / NEXT
===========================================================================
1. LIVE VERIFY (Carlos was going to test): Island -> Play -> press F in the dark -> beam on, F again -> off. Nothing has been
   seen on screen yet. Take a screenshot per memory "Confirm with a screenshot before 'fixed'"; editor screenshots lie
   unfocused (memory "Verification requires a build"), so prefer Carlos's own eyes or a build.
2. Tune the beam (all numbers still on the Light component, per interim doc prototyping rule). HAZE fog interaction:
   MRM-44 says test a plain spot light with HAZE alone BEFORE considering Volumetric Light Beam (installer unextracted).
3. Still in MRM-44 scope, not started: beam SWAY (little idle, more walking, a lot sprinting), visible breath (steam at
   screen centre when stamina < ~50%, enabled by the event director on mine entry), enemy visual detection (the player
   is more visible with the light on; hook = `MoonlightFlashlight.IsOn`/`Toggled`; changing how enemies detect is
   CLEANUP-adjacent per the interim doc, so tell Carlos before touching Spotter/Blaze code), "slight alpha showing the
   plastic" lamp model (art from Carlos), Scott's Sharpie name texture (Carlos).
4. Performance: after it works, ask Carlos to play a build and say "check the performance" (MRM-85 procedure), with and
   without the flashlight. Shadows are OFF on purpose (GPU-bound, shadow-atlas warnings). If shadows are wanted, make it
   a tunable and measure.
5. MoonlightTunables: gameplay numbers (sway amplitudes, breath threshold, detection multiplier) belong there when they
   arrive; beam look numbers can stay on the Light while prototyping.

===========================================================================
3. TRAPS ALREADY FOUND
===========================================================================
- The HQ flashlight is a WieldableTool (`Assets/ThirdParty/AST-046/HQFPS/Prefabs/Wieldables/HQFPS_Wieldable_Flashlight.
  prefab`, item "HQFPS_Flashlight") that equips a hand with a Switch animation. Carlos REJECTED that behaviour. Its FP FBXs
  and materials were never migrated (they live in the Weapon project, E:\playground\weapon, MCP port 8082). Do not
  re-propose it. PolymindPlayerBuild's KeepWieldables deliberately excludes it.
- The docs used to say F = Flashlight but the real input asset had F = Interact (Hold) and L = FlashlightToggle. Check the
  .inputactions, not the docs, before trusting a binding.
- Assets/ThirdParty/** is git-ignored: the cookie copy will not show in GitHub Desktop (expected, same as every vendor asset).
- MainMenu.unity had unsaved changes not from this work, so Island could not be loaded through the MCP without saving or
  discarding them. Don't decide that for Carlos.
- After the Unity refresh `M_Moon.mat` and `Mustasurma SDF.asset` showed as modified, unrelated to this work. Ask Carlos
  before committing them.
- UnityMCP: use the `UnityMCP` server (MrMoonlight@87580c9df5a077ae, port 8080). Light properties: set `color` as an
  {r,g,b,a} object and the cookie via the serialized name `m_Cookie` with {"path": ...}; `cookie` and array colours fail.
- Prefab edits: open_prefab_stage -> manage_gameobject create (with components_to_add) -> manage_components set_property ->
  save_prefab_stage -> close_prefab_stage, then grep the .prefab on disk to confirm (memory "Prefab asset edit traps").

===========================================================================
4. MODEL SUGGESTION
===========================================================================
Sonnet is right for the rest (tuning, sway, breath particle system). Enemy-detection wiring may be cleanup territory:
raise it with Carlos first.
