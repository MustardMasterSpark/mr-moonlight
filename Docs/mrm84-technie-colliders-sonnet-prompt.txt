Context prompt: MRM-84 Technie Collider Creator vegetation colliders (ongoing, not done).

WRITTEN: 2026-09-17. UPDATED: 2026-09-18. Read this instead of chat scrollback if resuming this
work cold.

===========================================================================
0. BRANCH — READ THIS FIRST
===========================================================================
Work happens on branch `mrm-84` now, NOT `main`. The "Add Technie Collider Creator 2 (AST-116)..."
commit had already landed on `main` (and been pushed to origin) before this was caught -
`mrm-84` was created off that commit on 2026-09-18; that commit was NOT rewound off main (would
need a force-push, Carlos's call, not done). All new collider work commits go on `mrm-84` from
here on. `git status` / `git branch --show-current` to confirm before doing anything.

===========================================================================
1. WHAT THIS IS
===========================================================================
Giving Island's vegetation trees tight, accurate colliders using Technie Collider Creator 2
(AST-116). Two real motivations, not just performance: (a) a live gameplay bug where a rifle shot
visually passed through a tree trunk because the old capsule collider didn't cover that geometry,
(b) Technie's automated VHACD ("Auto") mode explodes on leafy/branchy meshes (31,485 colliders
across 154 test objects in the worst measured case).

Everything happens in an isolated test copy: `Assets/_Project/Art/VegetationPrefabs/
AST116_ColliderTest/` and `Assets/_Project/Scenes/VegetationGallery_TechnieColliderTest.unity`.
The live `VegetationGallery.unity` and the original vegetation prefabs have NOT been touched
(after one real incident where they briefly were — restored from git, tool hardened with a
scene-path guard, see `Docs/performance-log.md` for the incident writeup).

===========================================================================
2. THE VALIDATED PROCESS — READ THIS FIRST
===========================================================================
Full step-by-step workflow: **`Docs/technie-vegetation-collider-process.md`**. Don't re-derive it;
that doc has the exact steps, the tool reference, and every lesson learned so far (including one
bad automated-split attempt that was recovered losslessly). Short version: Carlos rough-paints
each limb as one whole Convex Hull selection in Technie's tool (Prefab Mode, on the "Visual"
child), then hands off to Claude to split each at the mesh's real ring seams via
`AST116TechnieBatchCollider.SplitPaintedHullsAtRingSeams` (in
`Assets/_Project/Code/Editor/Migration/AST116TechnieBatchCollider.cs`).

===========================================================================
3. PROGRESS
===========================================================================
Done (manual-split process): `AP_ENV_tree_Nokmyung`, `AP_ENV_tree_SaGeeSukRim` (fully covered -
two branch tips looked uncovered on first review but that was a visual artifact of the mesh
renderer occluding the gizmo overlay, not a real gap).

Not started: ~106 remaining "had collider" species (rocks/logs/stumps don't need this - Auto/VHACD
already gives them a clean 1 collider each; this manual process is only for branchy trees).

===========================================================================
4. TRAPS ALREADY FOUND (don't rediscover these)
===========================================================================
- Colliders attach to the "Visual" child (the object with MeshFilter), never the root, for this
  asset library. Clicking the root and seeing no Collider component doesn't mean none exists.
- Two delete buttons look similar, do very different things: "Delete generated colliders and game
  objects" (top Tools row) is safe/repeatable. "🗙 All" in the Hulls list deletes the hull
  *definitions* (your painted work) - don't use it mid-process.
- Convex Hull is the CORRECT type for a pre-split limb, not a fallback - Auto/VHACD on an
  already-simple selection just adds noise. This is why Auto "never gives a good result" once you
  start manually splitting - it's the wrong tool for that stage, not a preset problem.
- `SplitPaintedHullsAtRingSeams` takes an optional `onlyHullNames` filter - use it when adding a
  missed limb after the fact, or it reprocesses already-split hulls and over-fragments them.
- Domain reloads (script recompiles) do NOT kick Prefab Mode / the Rigid Collider Creator window
  closed in this Unity version - confirmed safe to edit the script and recompile mid-session.
  Exiting Prefab Mode (Carlos navigating away / saving) does lose the live in-memory session
  though - re-check `PrefabStageUtility.GetCurrentPrefabStage()` before assuming state persists.
- Scene View screenshots render washed-out/gizmo-less when the Editor window isn't OS-focused -
  known quirk, not a bug, don't chase it - just note the limitation and ask Carlos to look himself.
- Before concluding a "missing collider" is a bug: compare total selected-triangle count across
  all hulls before vs after the operation in question. It never drops triangles - if the count
  matches, the gap was never painted in the first place.

===========================================================================
5. NEXT STEPS
===========================================================================
Continue tree by tree, same process. Carlos paints, tells Claude the prefab name is ready, Claude
runs the split, both review. No new tooling should be needed unless a new failure mode shows up -
if one does, update `Docs/technie-vegetation-collider-process.md`'s lessons-learned section, don't
just fix it silently.

Session hygiene: this file exists because Carlos used his "final instructions" trigger phrase
mid-session (work is continuing, not ending) - CLAUDE.md's process says to do this regardless.
Update this file's Progress section (§3) as more trees get done, rather than writing a new one
each time, until the big cleanup session or this issue closes.

===========================================================================
6. INCIDENTAL CROSS-ISSUE WORK DONE ON THIS BRANCH (2026-09-18)
===========================================================================
While waiting for the first painted tree this session, Carlos flagged that Main Menu's wind-sway
effect (built under MRM-18) was showing up on vegetation in Island/VegetationGallery too - not
just the two species he named as visibly swaying, but 11 shared materials / ~17 species, because
`_WindEnabled` was a flag on shared production material assets, not menu-scoped. Fixed on this
`mrm-84` branch (Carlos's explicit go-ahead: "change scenes and do whatever to fix this"):
duplicated the 11 wind-enabled materials as `_MainMenu` copies, turned wind off on all 11
originals, repointed the 7 actually-staged MainMenu renderers at the duplicates. Full writeup:
`Docs/retrolit-wind-sway.md` (2026-09-18 section). Logged as a comment on MRM-18, not a new
issue - flagging here per CLAUDE.md's cross-issue-scope-exception rule since it landed on the
`mrm-84` branch rather than its own.

If resuming cold: this is DONE and verified (read back `_WindEnabled` on both the original and
`_MainMenu` copy of `M_AP_AlaskaCedar_001_2_extra0` - confirmed 0 and 1 respectively). No open
thread here. The two scenes touched (`MainMenu.unity`, then back to
`VegetationGallery_TechnieColliderTest.unity`) were both saved properly; nothing was discarded.

**Forward-looking, no action yet:** Carlos is thinking about vegetation reacting to more than
wind eventually - weather states (windy, rainy) and destructible fire on trees were named
explicitly (2026-09-18) as future directions, not yet scoped. Keep this in mind if touching the
wind system again: the per-material-asset boolean flag is already the wrong shape (that's what
caused this leak) - don't extend that pattern for a second effect. See "Forward-looking" section
in `Docs/retrolit-wind-sway.md` for the specific caution.
