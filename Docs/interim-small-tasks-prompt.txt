Context prompt: INTERIM SMALL TASKS (reusable). Read this instead of the big handoff.

WRITTEN: 2026-09-16. Reuse it for any small, self-contained task Carlos picks while he builds 3D
models and the big cleanup session hasn't happened yet. When the big cleanup session starts, this
file retires. That session reads the "Interim work log" at the bottom.

===========================================================================
1. THE SITUATION IN FIVE LINES
===========================================================================
- Mr. Moonlight is a first-person horror shooter (Unity 6.3 URP, Windows, PSX look). The demo is
  playable; October (the Kickstarter demo) is the real quality bar.
- A BIG CLEANUP IS PLANNED BUT NOT STARTED. It will refactor code, remove unused packages, and
  rework the enemy system (Spotter / Blaze AI / DemoSpotterPopulationManager), the weapon system
  (PolymindGames FPSCore + HQ FPS Weapons) and audio (mixer, EnemyAudioHooks, vendor AudioManager).
  Its handoff is `Docs/demo-wrapup-audio-music-sonnet-prompt.txt`. DO NOT read that file for an
  interim task; its existence is all you need to know.
- Carlos is working on 3D models in parallel. These sessions exist so he can use his weekly token
  budget without stalling, so keep them short and cheap.
- Carlos is the only one who touches Unity. Scene/inspector work through the Unity MCP bridge needs
  his permission first (CLAUDE.md, kickstart.md §B.3).
- Never commit or push. Carlos uses GitHub Desktop.

===========================================================================
2. WHAT COUNTS AS AN INTERIM TASK
===========================================================================
Fair game (additive, self-contained, survives the cleanup untouched):
- New HUD/screen effects, UI polish, post-process additions.
- Trying out or integrating an asset that doesn't depend on a system scheduled for rework
  (collisions, VFX, environment props, sky, vegetation, shaders).
- Level dressing, staging and lighting, with permission.
- Small bug fixes that stay inside one component.

NOT an interim task. Stop and tell Carlos it belongs to the cleanup:
- Anything that changes how enemies think, spawn or take damage, how weapons fire or reload, or
  how audio is routed or mixed.
- Deleting packages, folders or vendor code, even if obviously dead (Burntwax, Simple Water Shader).
- Upgrading any installed package version (GUIDs are load-bearing, see `Docs/external-assets.md`).
- Refactoring existing code "while you're there."
If a task starts pulling you into one of these, stop, say so in one or two sentences, and offer
either a narrower version or parking it for the cleanup.

===========================================================================
3. HOW TO BUILD IT SO THE CLEANUP DOESN'T HAVE TO UNDO IT
===========================================================================
- Make it a drag-and-drop component or prefab. Don't hardcode anything to the Island scene.
- Put numbers in `MoonlightTunables` when they are gameplay values. Early visual/staging numbers
  can stay on the component while prototyping.
- Put new code in its own file under `Assets/_Project/Code/Runtime/<Area>/`. Don't edit vendor
  code in `_Project/Code/Vendor/`. If you'd have to, that's a cleanup task (see 2).
- Keep hooks one-directional: the new thing may listen to existing systems, but existing systems
  shouldn't be modified to call the new thing, except for a one-line hook you record in the log.
- New asset: evaluate in Playground first (`Docs/dual-project-workflow.md`), extract lean, and
  copy only what ships. Mechanics (rename, extract, GUID-match sync, verify, spreadsheet update)
  are all written up in `Docs/asset-import-update-process.md` (added 2026-09-17) — follow that
  instead of re-deriving it. Record the result in `Docs/external-assets.md` → "2026-09-16 audit"
  table.

===========================================================================
4. HOW TO RUN THE SESSION CHEAPLY
===========================================================================
1. Read ONLY: this file, `CLAUDE.md`, and the one or two docs the task names. Look up memories
   that match the task. Don't sweep the Docs folder.
2. Confirm the plan with Carlos in a few lines before building: what gets added, where, and how
   he'll test it.
3. LINEAR + BRANCH, before the first code or scene edit:
   - Umbrella issue: **MRM-81 "Interim small tasks before the big cleanup"**.
   - If the task already has its own Linear issue, use that issue and its branch as normal, and add
     "related to MRM-81" so the cleanup can find it.
   - If it has NO issue: create a **sub-issue of MRM-81** (team MrMoonlight, project
     MrMoonlightDemo, parent MRM-81) with a one-paragraph description and a "Done when" line.
     Tell Carlos the new ID and its git branch name (e.g. `mrm-82`). He creates the branch in
     GitHub Desktop. Don't ask whether to make one; this is the default.
   - Check `git status` and the current branch before editing anything.
   - Progress and findings go as comments on that sub-issue, not on MRM-81 itself.
4. Verify for real. Editor screenshots and "no console errors" aren't proof (see memory
   "Confirm with a screenshot before 'fixed'"). Anything visual or performance-related gets checked
   in a build or Play Mode with Carlos.
5. Wrap up with Carlos's "final instructions" in a light form:
   - commit message text for the sub-issue's branch;
   - a closing comment on the sub-issue (what changed, how it was verified, any hook into an
     existing system), then move it to In Review. Carlos decides Done;
   - one entry in the Interim work log below, with the sub-issue ID.
   Don't write a new handoff file for an interim task. The sub-issue + log entry ARE the handoff.

===========================================================================
5. WHERE TO LOOK THINGS UP
===========================================================================
- Assets (owned, wishlist, purpose, priority): `C:\Users\calva\Desktop\assets\ASSETS - Index
  2026-09-16.xlsx` is the editable master (outside the repo, `AST-###` IDs); `Docs/asset-index/
  ASSETS-Index.xlsx` is a git-tracked read-only mirror, added 2026-09-17. What's installed:
  `Docs/external-assets.md` → "2026-09-16 audit".
- Debug keys and cheats: `Docs/debug-tools.md`. Build process: memory "Build process".
- 3D/prop work: `/prop` wizard (`Docs/3d-prop-pipeline-wizard.md`).
- Player controller: `Assets/_Project/Prefabs/Player/Player_Tracey.prefab` is now a complete,
  self-contained drag-and-drop prefab (finished 2026-09-17 — it used to be missing debug toggles
  and a few systems that only existed on the Island scene instance). Burntwax FPS Engine is fully
  removed from the project as of the same date — don't propose removing it again, and don't treat
  it as still-present in any doc that predates 2026-09-17. See `Docs/external-assets.md` →
  "Removed" and the `Docs/changelog.md` MRM-9 entry for detail.

===========================================================================
6. TASK SLOT: Carlos fills this in, or states the task in chat
===========================================================================
Task: FLASHLIGHT + visible breath (Linear MRM-44, Sonnet). Carlos, 2026-09-18: "the next task is going to be the
  implementation of flashlight". Read the issue with get_issue MRM-44 first (it is the source of truth).
Asset(s) involved: HQ FPS Weapons 2.0 ships a Flashlight wieldable (`HQFPS/Art/Meshes/Wieldables/Flashlight/`:
  Flashlight.fbx, FP_Flashlight.fbx, FP_Arms_Flashlight.fbx with Equip/Unequip/Idle/Hold/Switch clips). Verify
  where it lives in Mr. Moonlight before relying on this. `F` (Flashlight Toggle) is bound in the input actions but
  not wired to gameplay (Docs/controls.md).
Rules that apply: a glowing/lit object gets a REAL Light on the prefab, never an emission map (CLAUDE.md). The
  wieldable system is PolymindGames (vendor, do not edit `_Project/Code/Vendor/`): add the flashlight as a new
  wieldable/prefab and a new script, hook one-directionally. If it needs changes inside the weapon system, that is
  the cleanup (section 2): stop and tell Carlos.
Performance heads-up (from MRM-85): the frame is GPU-bound and the log already shows URP shadow-atlas warnings
  ("24 shadow maps in 2048x2048"); an extra shadow-casting spot light costs real GPU time. Make shadows/range/
  intensity tunables and, once it works, ask Carlos to play a build and say "check the performance"
  (Docs/performance-sessions.md) to compare with and without the flashlight.
Linear issue: MRM-44.
Done when: per the MRM-44 description; verified live in Play Mode with a screenshot.

---------------------------------------------------------------------------
EXAMPLE TASK (the first one Carlos plans): better tree collisions with Technie Collider Creator 2
---------------------------------------------------------------------------
Spreadsheet row: "Technie Collider Creator 2", now AST-116 in the "OWNED (FROM WISHLIST)" section
(no longer plain wishlist — see Task Slot above). Starting facts, verify each before relying on it:
- Collidable vegetation species are GameObject + FloraInstanceRenderer prefabs in
  `Assets/_Project/Art/VegetationPrefabs/` (memory "MRM-70 vegetation + Flora pipeline"). The
  island has ~5,900 vegetation GameObject colliders and zero terrain trees (memory "Island NavMesh
  facts"). Decorative grass is TerrainDetail and has no colliders.
- Scope it as a prefab-level collider swap: generate better convex/compound colliders on the
  vegetation prefabs, so every placed instance benefits without touching the Gaia spawners.
- Evaluate in Playground first. Technie is an editor tool; ideally only the generated collider
  data ships, not the tool.
- Before touching anything, check whether the vegetation prefabs are baked into NavMesh
  obstacles and whether Spotter vision raycasts hit tree colliders. New collider shapes change
  both. If either would need enemy-AI changes, stop: that's the cleanup (section 2). Rebaking the
  NavMesh is fine with Carlos's permission.
- Measure before and after in a build (collider count, physics time in the profiler, FPS on the
  same route). MRM-64 wants real numbers for any optimization.
- Gaia respawn caveat: regenerating biomes re-instantiates prefabs, so a prefab-level change is
  durable but a per-instance edit isn't (memory "MRM-70 'generate new Gaia' trigger").

===========================================================================
INTERIM WORK LOG: append one entry per session, newest last
===========================================================================
Format: date · MRM-## (sub-issue of MRM-81) · task · files/prefabs/scenes touched · any hook added
into an existing system · anything the cleanup session must know.

2026-09-17 · MRM-84 · Evaluated Technie Collider Creator 2 (AST-116) for vegetation colliders on
an isolated copy (never touched the live scene/prefabs) · added `Assets/Technie/PhysicsCreator/`
(lean install, demo content skipped), `Assets/_Project/Art/VegetationPrefabs/AST116_ColliderTest/`
(154 duplicated prefabs + Play Mode test evidence), `Assets/_Project/Scenes/
VegetationGallery_TechnieColliderTest.unity`, `Assets/_Project/Code/Editor/Migration/
AST116TechnieBatchCollider.cs` (reusable batch tool) · no hook into any existing system · verdict:
REJECT for leafy vegetation (VHACD "Auto" exploded to 31,485 colliders / ~254x baseline, worst
case 820-1024 per tree), ADOPT candidate for solid props only (rocks/logs/stumps got a clean 1
collider each). Full numbers in `Docs/performance-log.md` and the MRM-84 comment. The
`AST116_ColliderTest` folder + test scene are safe to delete if Carlos rejects outright, or reuse
if he wants to try a capped VHACD preset / non-Auto hull type on rocks only.

2026-09-18 · MRM-84 (follow-up, same issue) · Technie verdict revised after the paint-based workflow: KEEP Technie as
the paint brush (choosing which triangles are wood); the collider is baked by `WoodColliderTool` into ONE non-convex
MeshCollider per prefab (109 prefabs, RAYTEST OK); Auto/VHACD stays rejected. New scene `Island_Legion.unity` uses them
(8,786 instances). Files: `Code/Editor/Migration/WoodColliderTool.cs`, `Scenes/Island_Legion.unity`, `Scenes/Island_Legion/
NavMesh-Island_Legion.asset`, `Code/Runtime/DevTools/SessionLog.cs` (build session log, MRM-85), hook `EnemyIdentity.
AnySpawned/AnyDespawned` (one-directional, added for the log), MainMenuController Legion button (MRM-18 cross-issue).
Cleanup must know: the wood-collider recipe in `Docs/technie-vegetation-collider-process.md`; MRM-85 perf findings
(corpses are never destroyed, GPU-bound) point at enemy death/gore = cleanup territory.

2026-09-18 · MRM-44 (first iteration, flashlight only; visible breath, sway, enemy detection NOT done) · flashlight =
a Spot Light child `Flashlight` under Player_Tracey/Body/Head/Camera with `MoonlightFlashlight` (F / D-Pad Up toggles the
light, instant, no animation, no wieldable; Carlos rejected the HQ hand-held Flashlight wieldable, the lamp will be a
chest-mounted model later) · files: `Code/Runtime/Player/MoonlightFlashlight.cs`, `Prefabs/Player/Player_Tracey.prefab`
(new child), `InputSystem_Actions.inputactions` (+ regenerated .cs: F moved from Interact to FlashlightToggle, L binding
removed; Interact now has NO keyboard binding, Carlos picks it later), `Docs/controls.md`, cookie texture
`ThirdParty/AST-046/HQFPS/Art/Meshes/Wieldables/Flashlight/Materials/Flashlight_Cookie.png` (copied from the Weapon
project, GUID preserved) · no hook into existing systems, `MoonlightFlashlight.Toggled`/`IsOn` are the hooks for the
future enemy-detection change · cleanup must know: the HQ flashlight wieldable (HQFPS_Wieldable_Flashlight.prefab, item
"HQFPS_Flashlight") is deliberately NOT in Tracey's holster and its FP FBX/materials were never migrated; shadows on the
beam are OFF (GPU-bound frame, MRM-85), tunables still live on the Light component. Live F-key test is Carlos's.
Handoff for the next session: Docs/mrm44-sonnet-prompt.txt.

2026-09-19 · MRM-44 (second and third sessions: flashlight tuning + the hands' lighting; still NOT done: beam sway, visible
breath, enemy detection) · flashlight numbers are now `Flashlight*` tunables with a Live tuning block; hands and weapons
moved to a new URP rendering layer `ViewModel`, lit by their own directional light that follows the live sun (night floor),
plus world lights within 15 m (periodic scan) · files: `Code/Runtime/Player/MoonlightViewModelLighting.cs` (new),
`MoonlightFlashlight.cs`, `MoonlightTunables.cs` + asset, `Code/Runtime/DevTools/SessionLog.cs` (v4: flashlight / handLights
in [PERF]; cross-issue MRM-85), `Prefabs/Player/Player_Tracey.prefab` (ViewModelLight child), `ProjectSettings/TagManager.asset`
(rendering layer 1 renamed ViewModel), 21 HQ mask maps copied into `Assets/ThirdParty/AST-046` (git-ignored) and the two arm
materials' `_SmoothnessIntensity` set to 0.25 (cross-issue MRM-9) · hooks added: `MoonlightViewModelLighting.WorldLightsOnHands`
(read by SessionLog) · cleanup must know: read `Docs/lighting.md` and `Docs/viewmodel-light-layers.md` first; URP lighting reads
`UniversalAdditionalLightData.renderingLayers` not `Light.renderingLayerMask`; three lighting tunables are unwired (listed in
lighting.md section 4). Next: lighting rework, `Docs/lighting-rework-opus-prompt.txt`.

2026-09-19 (later) · MRM-44 follow-up / process · git: `mrm-44` had been created tracking `origin/main`, so both MRM-44 commits went straight
to `main`; upstream removed, rule added to CLAUDE.md (branches are created with `--no-track` and the upstream checked), `mrm-86` created clean
for the new lighting issue **MRM-86** (Opus, `Docs/lighting-rework-opus-prompt.txt`). Change record C-002 in `Docs/performance-sessions.md` §8.
